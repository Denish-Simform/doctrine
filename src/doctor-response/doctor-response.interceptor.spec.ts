import { DoctorResponseInterceptor } from './doctor-response.interceptor';

describe('DoctorResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new DoctorResponseInterceptor()).toBeDefined();
  });
});
