import { PatientResponseInterceptor } from './patient-response.interceptor';

describe('PatientResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new PatientResponseInterceptor()).toBeDefined();
  });
});
